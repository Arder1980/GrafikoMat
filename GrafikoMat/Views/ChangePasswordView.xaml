<UserControl
    x:Class="GrafikoMat.Views.ChangePasswordView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12" Width="380">
			<TextBlock Text="Wymagana zmiana hasła"
                       FontSize="24"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
			<TextBlock Text="Ze względów bezpieczeństwa, musisz ustawić nowe hasło przy pierwszym logowaniu."
                       Opacity="0.8"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,0,0,12"/>

			<PasswordBox x:Name="NewPasswordBox"
                         Header="Nowe hasło"
                         Password="{x:Bind ViewModel.NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
			<PasswordBox x:Name="ConfirmPasswordBox"
                         Header="Potwierdź nowe hasło"
                         Password="{x:Bind ViewModel.ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

			<Button Content="Zapisz i kontynuuj"
                    Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Command="{x:Bind ViewModel.SavePasswordCommand}"
                    Margin="0,12,0,0"/>

			<InfoBar x:Name="ErrorBar"
                     IsOpen="False"
                     IsClosable="True"
                     Severity="Error"
                     MinHeight="72"/>
		</StackPanel>
	</Grid>
</UserControl>