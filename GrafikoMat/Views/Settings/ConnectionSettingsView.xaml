<UserControl
    x:Class="GrafikoMat.Views.Settings.ConnectionSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:GrafikoMat.Controls"
    mc:Ignorable="d">

    <controls:ActionContainer x:Name="ActionContainer">
        <StackPanel Spacing="20" MaxWidth="500" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Połączenie z Bazą Danych" FontSize="20" FontWeight="SemiBold"/>

            <TextBlock TextWrapping="Wrap">
                 Wprowadź tutaj dane połączeniowe do Twojego projektu w Supabase. Te ustawienia są globalne dla całej aplikacji.
            </TextBlock>

            <TextBox x:Name="UrlTextBox" Header="Supabase URL"/>
            <TextBox x:Name="ApiKeyTextBox" Header="Supabase Anon Key"/>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="TestButton" 
                        Content="Testuj połączenie"
                        Click="TestButton_Click"/>
                <Button x:Name="SaveButton" 
                        Content="Zapisz i kontynuuj"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </StackPanel>
    </controls:ActionContainer>
</UserControl>