<UserControl
    x:Class="GrafikoMat.Controls.ActionContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:media="using:Microsoft.UI.Xaml.Media"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">
    <Grid>
        <ContentPresenter x:Name="ContentHost" Content="{x:Bind Content, Mode=OneWay}"/>

        <Grid x:Name="OverlayHost" Visibility="Collapsed" Canvas.ZIndex="1">

            <Grid>
                <Grid.Background>

                    <media:AcrylicBrush
                        TintOpacity="0.1"
                        TintColor="{ThemeResource SystemChromeAltLowColor}"/>
                </Grid.Background>
            </Grid>
       
            <controls:ProgressRing x:Name="ActionProgressRing" IsActive="False" Width="48" Height="48"/>

            <controls:InfoBar x:Name="ActionInfoBar"
                     IsOpen="False"
                     IsClosable="False"
                     HorizontalAlignment="Center"
      
                VerticalAlignment="Center"
                     MinWidth="340"
                     MaxWidth="500">
                <controls:InfoBar.Content>
                    <StackPanel Padding="12">
                        <Grid x:Name="ErrorActionGrid" ColumnSpacing="8" Margin="0,12,0,0" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="CopyButton" Grid.Column="1" Content="Kopiuj treść błędu" Click="CopyButton_Click"/>
                            <Button x:Name="OkButton" Grid.Column="2" Content="OK" Style="{StaticResource AccentButtonStyle}" Click="OkButton_Click"/>
                        </Grid>
                    </StackPanel>
                </controls:InfoBar.Content>
            </controls:InfoBar>
        </Grid>
    </Grid>
</UserControl>