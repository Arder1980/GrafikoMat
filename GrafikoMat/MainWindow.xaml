<Window
    x:Class="GrafikoMat.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" RowDefinitions="Auto,100,*,Auto,Auto" Background="Transparent">
        <Grid x:Name="TopBarRow" Grid.Row="0" Height="32"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              Padding="10,0" Margin="0,0,140,0">
            <Grid x:Name="DragBar" VerticalAlignment="Center">
                <TextBlock Text="GrafikoMat Dyżurowy" FontSize="14" FontWeight="SemiBold" Opacity="0.92"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="1" Padding="12,8" MinHeight="100">
            <TextBlock Text="{x:Bind ViewModel.UnitName, Mode=OneWay}" FontSize="14" Opacity="0.9" TextWrapping="WrapWholeWords"/>
        </Grid>

        <Grid Grid.Row="2" Padding="12,8,12,8">
            <Border BorderBrush="#1A000000" BorderThickness="1" CornerRadius="6" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
                <Grid x:Name="ViewportGrid">
                    <ContentControl x:Name="ViewportCurrent" Opacity="1"
                                    HorizontalContentAlignment="Stretch"
                                    VerticalContentAlignment="Stretch"/>
                    <ContentControl x:Name="ViewportNext" Opacity="0"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Padding="12,4">
            <ItemsControl ItemsSource="{x:Bind Actions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <StackPanel.ChildrenTransitions>
                                <TransitionCollection>
                                    <EntranceThemeTransition/>
                                    <RepositionThemeTransition/>
                                </TransitionCollection>
                            </StackPanel.ChildrenTransitions>
                        </StackPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Label}"
                                Command="{Binding Command}"
                                Margin="6,0,0,0"
                                MinHeight="28"
                                MinWidth="128"
                                Padding="12,6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <Grid Grid.Row="4" Padding="10,4" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="16"
              Background="{ThemeResource LayerFillColorDefaultBrush}">
            <TextBlock Text="{x:Bind ViewModel.EngineName, Mode=OneWay}" Opacity="0.8" FontSize="12"/>
            <Border Grid.Column="1" Width="1" Background="#22000000"/>
            <TextBlock Grid.Column="2" Text="{x:Bind ViewModel.PriorityOrder, Mode=OneWay}" Opacity="0.8" FontSize="12"/>
        </Grid>
    </Grid>
</Window>